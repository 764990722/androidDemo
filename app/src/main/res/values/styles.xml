<resources>

    <!-- APP主题 -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/blue</item>
        <item name="colorAccent">@color/blue</item>
    </style>

    <style name="StartTheme" parent="AppTheme">//启动页图片
        <item name="android:windowBackground">@mipmap/launch_house</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <!-- 加载框 -->
    <style name="AppTheme.Loading" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@color/translucent</item>
    </style>

    <!--加载半透明的dialog-->
    <style name="dialog1" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 显示对话框时当前的屏幕是否变暗 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!--<item name="android:backgroundDimAmount">0.2</item>-->
        <!-- 模糊 -->
    </style>

</resources>
